```javascript
function unlockIdleCompute() {
  const resources = {
    gamingPCs: {
      status: "often idle",
      potential: significant,
    },
    miningRigs: {
      status: "underutilized",
      potential: vast,
    },
    smartphones: {
      status: "idle",
      potential: untapped,
    },
    dataCenters: {
      status: "excess capacity",
      potential: substantial,
    },
  };

  const impact = harnessResources(resources);

  if (impact) {
    // Result: Doubled world's AI capacity
    console.log("Enabling more experimentation, faster iteration, and broader access");
  }

  function decentralizationBenefits() {
    const marketplace = {
      resourceProviders: {
        monetizeIdleHardware: true,
      },
      developers: {
        accessAffordableCompute: true,
      },
      networkOperators: {
        rewardedForFacilitatingTransactions: true,
      },
    };

    const ecosystem = {
      resilience: high,
      efficiency: optimal,
      resourceAllocation: marketDriven,
    };

    // Outcome: Decentralization fosters a more robust and efficient compute ecosystem
    console.log("Aligning incentives for resource sharing and optimal allocation");
  }
}
```
